# Mental Models

This file contains intuition-first explanations.

Example:

transaction.atomic()

Everything succeeds together,
or everything disappears together.

But it does NOT prevent concurrent reads.